@model AppTarefas.Models.Status

@{
    ViewData["Title"] = "Deletar";
}

<h1>Deletar status</h1>

<h3 class="alert alert-danger">Você realmente deseja apagar este status?</h3>
<div>
    <h4>Status</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.StatusNome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.StatusNome)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="StatusId" />
        <input type="submit" value="Deletar" class="btn delete fw-bold text-white" />
        <a asp-action="Index" class="btn btn-secondary text-white fw-bold">Voltar</a>
    </form>
</div>
